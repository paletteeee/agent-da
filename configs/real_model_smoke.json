{
  "tasks": [
    {
      "task_id": "real_model_smoke_001",
      "agent_id": "agent_model",
      "system_prompt": "Use the memory tools for every memory operation. When deriving a memory, provide the actual source_ids.",
      "prompt": "Store a generic source fact using memory_id fixture_source, then derive memory_id fixture_derived from fixture_source.",
      "seed": 0,
      "temperature": 0.0,
      "max_steps": 8,
      "acceptance": {
        "required_event_kinds": ["memory_write", "memory_derive"],
        "required_memory_ids": ["fixture_source", "fixture_derived"],
        "required_provenance": [{"derived_id": "fixture_derived", "source_id": "fixture_source"}]
      }
    }
  ]
}
